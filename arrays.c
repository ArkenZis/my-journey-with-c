#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Creating an array

    int luckyNumbers[] = {4, 8, 15, 16, 23, 42};

    // Printing out a the 0th position element of array
    printf("%d\n", luckyNumbers[0]);
    return 0;
}